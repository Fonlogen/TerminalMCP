name: CI

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:

jobs:
  test:
    name: ${{ matrix.os }} / node ${{ matrix.node }}
    runs-on: ${{ matrix.os }}
    strategy:
      # Report every platform rather than stopping at the first failure:
      # Windows and macOS paths differ enough that one result hides the others.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        node: ['18', '20', '22']
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      # No install step: the server has zero dependencies, which is worth
      # asserting rather than assuming.
      - name: Assert there are no dependencies
        shell: bash
        run: |
          node -e '
            const p = require("./package.json");
            const n = Object.keys(p.dependencies || {}).length
                    + Object.keys(p.devDependencies || {}).length;
            if (n > 0) { console.error("Expected 0 dependencies, found " + n); process.exit(1); }
            console.log("0 dependencies, as intended");
          '

      - name: Syntax check every source file
        shell: bash
        run: |
          set -e
          for f in src/*.js src/tools/*.js bin/*.js test/*.mjs scripts/*.mjs; do
            node --check "$f"
          done
          echo "all files parse"

      - name: Environment report
        run: node bin/terminalmcp.js --doctor

      # The browser suite drives a real Chromium. GitHub's runners ship Chrome,
      # and the suite skips itself cleanly when no browser is present, so this
      # is a convenience rather than a requirement.
      - name: Report which browser was found
        shell: bash
        run: |
          node -e '
            import("./src/cdp.js").then(({ findBrowser }) => {
              try {
                const b = findBrowser(null);
                console.log("browser: " + b.path + " (" + b.kind + ", via " + b.source + ")");
              } catch (err) {
                console.log("no browser found; test:browser will skip");
              }
            });
          '

      - name: Test suite
        run: npm test
